-- ============================================================
-- Create the cleaned analytics layer
-- Raw source tables remain unchanged in the raw schema.
-- ============================================================

CREATE SCHEMA IF NOT EXISTS analytics;